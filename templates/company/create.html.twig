{% extends "./base.html.twig" %}

{% block content %}
  <div class="container mt-5">
      <div class="row justify-content-center">
          <div class="col-md-6">
              <!-- Bootstrap Card -->
              <div class="card">
                  <div class="card-header">
                      <h5 class="card-title">Company |  {{ form_type }}</h5>
                  </div>
                  <div class="card-body">
                      {{ form(form) }}
                  </div>
              </div>
          </div>
      </div>
  </div>
{% endblock %}